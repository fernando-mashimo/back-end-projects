const userController = require('./User.controller');
const categoryController = require('./Category.controller');
const postController = require('./BlogPost.controller');

module.exports = {
  userController,
  categoryController,
  postController,
};